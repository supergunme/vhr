# vhr 微人事项目改造记录

> 本文档记录本次会话中客户（Steven）提出的所有需求，以及对应执行的分析、操作和代码修改，用于向客户讲解。
>
> **文档分两部分：第一部分=简化改造（移除中间件）；第二部分=版本升级重构（Spring Boot + Vite）。**

---

# 第一部分：简化改造（移除中间件）

---

## 一、需求与操作时间线（按对话顺序）

### 阶段 0：项目分析（用户提示词）

> **用户**：分析这个代码库并创建一个 CODEBUDDY.md 文件，供后续使用。

**操作：**
- 检查无现存 `CODEBUDDY.md` / `AGENTS.md`，仅有 `README.md`。
- 通读 Maven 多模块结构、Spring Security 动态权限模型、前端动态路由机制。
- 新建 `CODEBUDDY.md`，记录构建/运行/测试命令、模块分层、安全授权模型、前端约定等。

---

### 阶段 1：中间件分析（用户提示词）

> **用户**：工程要做得简单，前后端分离，保持现有架构但简化组件。先分析除数据库外其他中间件都做什么、是否可移除、影响哪些功能。

**操作（只分析，未改代码）：** 逐个排查每个中间件的实际调用点，给出结论：

| 组件 | 作用 | 唯一用途 / 调用点 | 移除影响 |
|------|------|------------------|---------|
| **Redis** | Spring Cache 缓存后端 | `MenuService.getAllMenusWithRole()` 的 `@Cacheable`（每次请求查权限用） | 无功能损失，可换内存缓存 |
| **RabbitMQ** | 异步邮件投递通道 | `EmployeeService.addEmp()` 发消息 + `MailSendTask` 定时重发 | 失去"添加员工后发欢迎邮件" |
| **mailserver** | 独立邮件消费服务 | 消费 MQ 消息发邮件 | 随 MQ 一起 |
| **WebSocket** | HR 在线聊天（框架内置，非外部中间件） | `WsController` + 前端 stomp | 失去在线聊天 |
| **FastDFS** | 分布式文件存储 | `HrInfoController` 头像上传 | 头像功能（可改本地磁盘保留） |

**结论**：最简架构 = `Vue 前端 + Spring Boot 单体（仅依赖 MySQL）`。

---

### 阶段 2：执行简化改造 + 启动（用户提示词）

> **用户**：Redis 改为内存方案；邮件、聊天功能都删除；FastDFS 移除；最终仅保留前后端服务 + MySQL。改完编译并启动。MySQL 已建好（localhost:3306，用户名密码均为 root）。

**操作：** 见下方"二、代码修改清单"。改造后编译、启动、验证全部通过。

---

### 阶段 3：问题排查（用户提示词）

> **用户**：人事管理模块的表单没有显示出来。

**操作：**
- 用浏览器（playwright）实际登录系统（admin / 123）逐页验证。
- 定位结论：**"人事管理"下的 5 个子页面（员工资料/奖惩/培训/调薪/调动，即 `PerEmp/PerEc/PerTrain/PerSalary/PerMv`）在 vhr 原始脚手架里本就是空占位页**，每个 `.vue` 仅有一行文字，作者从未实现。
- 反证：同一套机制下，"员工资料 → 基本资料"(`EmpBasic.vue`) 完整渲染表格 + 搜索 + 增删改 + 分页（617 条数据正常加载），证明渲染、路由、接口、数据库均正常，简化改造未破坏任何功能。
- 控制台仅有的报错均与业务无关（HMR 探测 8080、失效的示例头像外链）。

---

### 阶段 4：本次文档（用户提示词）

> **用户**：把所有 prompt、操作和修改记录下来，用于给客户讲解。

→ 即本文档。

---

## 二、代码修改清单

### 改造 1：移除 Redis → 内存缓存

- `vhr/vhrserver/vhr-service/pom.xml`：删除 `spring-boot-starter-data-redis` 依赖。
- `vhr/vhrserver/vhr-web/src/main/resources/application.yml`：删除 `redis` 配置段。
- **缓存方案**：保留 `@EnableCaching` / `@Cacheable` / `cache-names: menus_cache`，Spring Boot 在无 Redis 时自动降级为内存缓存 `ConcurrentMapCacheManager`。权限逻辑完全不变。

### 改造 2：删除邮件功能（含 RabbitMQ 与 mailserver）

**删除文件/目录：**
- 整个 `vhr/mailserver/` 模块
- `vhr-service`：`config/RabbitConfig.java`、`task/MailSendTask.java`、`service/MailSendLogService.java`
- `vhr-mapper`：`MailSendLogMapper.java`、`MailSendLogMapper.xml`
- `vhr-model`：`MailConstants.java`、`MailSendLog.java`

**修改文件：**
- `vhr/pom.xml`：父 POM 移除 `<module>mailserver</module>`。
- `vhr-service/pom.xml`：移除 `spring-boot-starter-amqp` 依赖。
- `EmployeeService.java`：`addEmp()` 去掉 RabbitMQ 发消息 + 邮件日志逻辑，只保留入库。
- `VhrApplication.java`：移除 `@EnableScheduling`（仅 MailSendTask 用过定时）。

### 改造 3：删除聊天功能（WebSocket）

**后端删除：** `WsController.java`、`WebSocketConfig.java`、`ChatController.java`、`model/ChatMsg.java`；`vhr-web/pom.xml` 移除 `spring-boot-starter-websocket`。

**前端删除：** `src/components/chat/`、`src/views/chat/`、`src/utils/stomp.js`、`src/utils/sockjs.js`。

**前端修改：**
- `src/store/index.js`：精简，移除聊天/WebSocket 相关 state、mutations、actions。
- `src/utils/menus.js`：移除 `store.dispatch('connect')`。
- `src/router.js`：移除 `/chat` 路由与 `FriendChat` 引用。
- `src/views/Home.vue`：移除右上角聊天铃铛按钮与 `goChat` 方法。

### 改造 4：移除 FastDFS（头像改本地磁盘存储）

**删除：** `config/FastDFSUtils.java`、`resources/fastdfs-client.properties`；`vhr-web/pom.xml` 移除 `fastdfs-client-java` 依赖；`application.yml` 删除 `fastdfs` 配置。

**修改/新增（保留头像功能）：**
- `HrInfoController.java`：`/hr/userface` 上传改为存本地 `./userface/` 目录，返回 `/userface/xxx` 访问路径。
- 新增 `config/WebMvcConfig.java`：把本地 `./userface/` 映射为静态资源 `/userface/**`。
- `SecurityConfig.java`：放行 `/userface/**`。

### 改造 5：数据库配置 + 前端依赖修复

- `application.yml`：数据源改为 `localhost:3306/vhr`，用户名/密码 `root`/`root`。
- `vuehr/package.json`：删除 `node-sass` 和 `sass-loader`（项目实际未使用 sass，且 node-sass 4.x 无法在 Node 25 编译，导致 `npm install` 失败）。

---

## 三、最终架构

```
浏览器
  │
  ▼
Vue 前端 (vuehr, 开发端口 8082→代理到8081)
  │  axios / 动态路由
  ▼
Spring Boot 单体后端 (vhr-web, 端口 8081)
  │  MyBatis
  ▼
MySQL (localhost:3306/vhr, Flyway 自动建表)
```

**已彻底移除的外部依赖：** Redis、RabbitMQ、mailserver、FastDFS。

---

## 四、验证结果

- **后端**：`mvn clean package` 成功；启动后 Flyway 自动建表到 `vhr` 库；运行于 `http://localhost:8081`；`/index.html` 返回 200，未认证接口正确返回 401。无任何 Redis/MQ/FastDFS 报错。
- **前端**：`npm install` 修复后成功；`npm run serve` 编译成功，运行于 `http://localhost:8082`（8080 被占用自动改用），代理转发后端正常。
- **端到端**：浏览器登录 admin/123 成功，菜单完整加载，员工"基本资料"页表格 + 增删改 + 分页（617 条）全部正常。

---

## 五、待确认 / 已知情况

1. **"人事管理"5 个子页面为空** —— vhr 脚手架原始状态，作者未实现，非 bug。如客户需要可另行开发。
2. `HrService.getAllHrsExceptCurrentHr()` 现无调用方但保留（不影响运行）。
3. 数据库 `mail_send_log` 表保留（无害）。
4. 数据库里示例员工头像为失效外链，控制台会有 404，不影响功能。

---

# 第二部分：版本升级重构（Spring Boot + Vite）

> 目标：把后端 Spring Boot 和前端构建工具升级到 2026 年可维护的版本，**不影响现有功能**，最大程度解决前端向下兼容问题。

## 一、关键约束（客户提出）

- **客户是银行，生产环境必须保持 JDK 1.8** —— 最硬的约束。
- 因此 **Spring Boot 3.x 被排除**（强制要求 JDK 17+），后端只能用 JDK 8 下的最高版本。
- 遇到 sudo/交互操作时，交给用户在终端执行（如 JDK 安装）。

## 二、最终版本决策

| 项 | 升级前 | 升级后 | 说明 |
|----|--------|--------|------|
| JDK | 8 | **保持 8**（Zulu 8.94 / 1.8.0_492 arm64） | 银行约束 |
| Spring Boot | 2.4.0 | **2.7.18** | JDK8 下可用的 2.x 末版 |
| mybatis-spring-boot-starter | 2.1.0 | **2.3.2** | 兼容 SB2.7 |
| druid-spring-boot-starter | 1.1.10 | **1.2.20** | — |
| flyway | 内置旧版 | **8.5.13 + flyway-mysql** | SB2.7 BOM 管理 |
| POI | 4.1.1 | **4.1.2** | 保守升级，保 JDK8 |
| mysql 驱动 | BOM 管理 | **显式 8.0.33** | SB2.7 不再管理旧坐标版本 |
| 前端框架 | Vue 2 + ElementUI | **保持不动**（31 组件零改写） | 降低风险 |
| 前端构建工具 | vue-cli 3 (webpack) | **Vite 5.4** | 解决 Node 25 兼容 |
| 浏览器兼容 | — | **@vitejs/plugin-legacy** | 兼容银行老浏览器 |

**为什么低风险**：2.4→2.7 仍是 javax（非 jakarta），`WebSecurityConfigurerAdapter` 仅 @Deprecated 仍可用，SecurityConfig 无需重写；前端不动组件，只换打包器。

## 三、后端改动文件

- `vhr/pom.xml`：SB 2.4.0 → 2.7.18
- `vhr/vhrserver/vhr-mapper/pom.xml`：mybatis 2.3.2、druid 1.2.20、**新增 flyway-mysql**
- `vhr/vhrserver/vhr-service/pom.xml`：poi 4.1.2
- `vhr/vhrserver/vhr-web/pom.xml`：mysql-connector-java 显式 8.0.33
- Java 源码：**零改动**（javax / SecurityConfig 全部保持）

## 四、前端改动文件

- `vuehr/package.json`：移除 vue-cli/webpack 相关；新增 vite、@vitejs/plugin-vue2、@vitejs/plugin-legacy、terser；scripts 改 vite；加 `"type": "module"`
- `vuehr/vite.config.js`：**新建**，替代 vue.config.js（Vue2 插件 + legacy + 按后端路径前缀精确代理到 8081）
- `vuehr/index.html`：从 public/ 迁到项目根目录，改 Vite 入口语法
- `vuehr/babel.config.js`、`vue.config.js`、`public/index.html`：删除
- `vuehr/src/utils/menus.js`：webpack `require([...],resolve)` → Vite `import.meta.glob` 动态加载（前端唯一组件外逻辑改动）

## 五、升级中遇到并解决的问题

1. **JDK8 安装**：本机只有 JDK25/17。temurin@8 是 Intel 版需 Rosetta，改用 `brew install --cask zulu@8`（arm64 原生，用户手动 sudo 安装）。
2. **mysql 驱动缺版本**：SB2.7 BOM 不再管理旧坐标 `mysql:mysql-connector-java` → 显式指定 8.0.33。
3. **Flyway 不支持 MySQL**：Flyway 8.x 把 MySQL 支持拆到独立模块 → 补加 `flyway-mysql` 依赖。
4. **Vite 代理陷阱**：不能像 vue-cli 那样代理根路径 `/`（会吞掉 Vite 自身模块）→ 改为按后端路径前缀（/hr /employee /system /salary /doLogin /logout /verifyCode）精确代理。
5. **plugin-vue2 导入**：导出是 default 函数，不是具名 createVuePlugin。
6. **node-sass/sass-loader**：项目实际未用 sass，已在第一部分删除，避免 Node25 编译失败。

## 六、验证结果（全部通过）

- **后端**：JDK8（字节码 52.0）编译打包成功；启动无 ERROR；Druid 连接池、Flyway 校验（schema vhr v1）、MyBatis 全部正常；接口 /index.html→200、/hr/info→401、/verifyCode→200。
- **前端**：Vite 5.4.21 启动成功（端口 8088，因 8080 被腾讯会议占用）；main.js/menus.js 模块转译正常；代理后端正常；浏览器实测登录页完整渲染，控制台无错误。

## 七、当前运行环境

- 后端：`http://localhost:8081`（JDK8 + SB2.7.18）
- 前端：`http://localhost:8088`（Vite dev server，代理到 8081）
- 启动命令：
  - 后端：`JAVA_HOME=$(/usr/libexec/java_home -v 1.8) java -jar vhrserver/vhr-web/target/vhr-web-0.0.1-SNAPSHOT.jar`
  - 前端：`cd vuehr && npm run dev`

## 八、说明

- 端口 8080 被本机腾讯会议占用，前端临时用 8088；生产部署不受影响（可配回 8080）。
- 本次未做生产部署（用户确认暂不需要）。
